// app/admin/login/page.tsx
// Página de login admin — token introduzido via formulário POST, nunca pela URL.

import LoginClient from './LoginClient'

export default function AdminLoginPage() {
  return <LoginClient />
}
